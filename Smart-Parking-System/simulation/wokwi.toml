[wokwi]
version = 1
firmware = '.pio/build/esp32dev/firmware.bin'
elf = '.pio/build/esp32dev/firmware.elf'

# Optional: open http://localhost:8180/ to reach the simulated ESP32 web server.
[[net.forward]]
from = "localhost:8180"
to = "target:80"
